# This is a comment
# Skippy can be used for sending a sequence of SCPI commands to instrument in *txt, *csv formats.
# Optional arguments: --address --delay --post-operation-commands
*COUNT?
SAVE SCREENSHOT